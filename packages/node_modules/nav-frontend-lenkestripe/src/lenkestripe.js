import React, { Component, PropTypes as PT } from 'react';

class NavFrontendLenkestripe extends Component {
    render() {
        const { children } = this.props;
        return (
            <div className="nav-frontend-lenkestripe">
                <h2>Hello from nav-frontend-lenkestripe</h2>
                {children}
            </div>
        );
    }
}

NavFrontendLenkestripe.propTypes = {
    children: PT.node
};

export default NavFrontendLenkestripe;
